// Idris commands should not work here
// Open the command palette `ctrl+shift+p` and type `idris`
// If you can't see any `Language Idris` commands it's working
// correctly

function add(a, b)
{
    return a + b;
}
